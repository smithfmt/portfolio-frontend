---



const { title, subtitle } = Astro.props;
const testimonials = ["","",""];
---
<section id="testimonials-section" class="flex flex-col w-full items-center justify-center gap-16 py-32 h-fit">
    <h1 class="text-5xl font-black">Testimonials</h1>
    <div class="flex gap-16">
        {testimonials.map((t,i) => {return (
            <div class="relative top-0 grid items-center justify-center p-16 border-neutral-50 border rounded-lg shadow-glow-white hover:-top-4 transition-all">
                <h1 class="text-xl font-medium">"Quote"</h1>
                <h2 class="text-lg font-black">Name</h2>
            </div>
        )})}
    </div>
</section>