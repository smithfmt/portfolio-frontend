---
import {Icons} from "@assets/images/Images";
import Wordmap from "@components/graphics/wordmap/Wordmap";

import LinkedIn from "@components/icons/LinkedIn";
type Props = {
	title: string,
	body: string,
}

const { title, body } = Astro.props;
---
<section id="intro-section" class="flex w-full items-center justify-center gap-32 py-32 h-fit">
    <div class="relative grid gap-4 text-3xl border-neutral-50 p-16 border-t border-b border-glow-white shadow-glow-white-x w-full h-[30rem] ">
        <!-- <h1 class="font-black">
            {title}
        </h1>
        <p class="text-lg">{body}</p> -->
        <Wordmap client:load/>
    </div>
</section>